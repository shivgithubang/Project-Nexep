// Prisma schema removed: project migrated to Mongoose
// The previous Prisma schema has been intentionally removed during the migration
// to Mongoose. If you need to reference the old schema, check your Git history.

// NOTE: This file is left as a placeholder to avoid confusion. Database access is
// now handled via Mongoose in `lib/mongoose.js` and the `models/` directory.
